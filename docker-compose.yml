version: '2'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www/html
      - ./:/docker/
    volumes_from:
      - storage
  storage:
    build:
      context: docker/storage/
    volumes:
      - /storage